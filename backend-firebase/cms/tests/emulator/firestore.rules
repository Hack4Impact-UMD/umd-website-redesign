rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Synthetic relation tests only. This file is never deployed.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
