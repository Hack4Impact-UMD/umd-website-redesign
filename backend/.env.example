HOST=0.0.0.0
PORT=8082
APP_KEYS=myKeyA,myKeyB

# Cloudinary Configuration (Required for image uploads)
# Images are stored on Cloudinary to avoid Heroku's ephemeral filesystem
#
# Option 1: Heroku Add-on (automatic - no config needed)
# When Cloudinary is installed as a Heroku add-on, CLOUDINARY_URL is set automatically
# CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
#
# Option 2: Manual setup (for local development)
# CLOUDINARY_NAME=your_cloud_name
# CLOUDINARY_KEY=your_api_key
# CLOUDINARY_SECRET=your_api_secret

# Production (Heroku) Configuration
# DATABASE_URL=postgres://user:password@host:port/database
# MY_HEROKU_URL=https://your-app.herokuapp.com
